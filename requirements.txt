# Minimal requirements for Thai Food Nutrition Analyzer
# ความต้องการขั้นต่ำสำหรับแอปพลิเคชัน
# ใช้เมื่อการติดตั้งแบบเต็มไม่สำเร็จ

# Core web framework (จำเป็น)
streamlit>=1.25.0,<2.0.0

# Data manipulation (จำเป็น)
pandas
numpy

# HTTP requests (จำเป็น)
requests

# Text processing (แนะนำ)
fuzzywuzzy
python-levenshtein

# Utilities (แนะนำ)
python-dotenv
tqdm

# Data visualization (ทำงานได้โดยไม่มี)
plotly
matplotlib

# File handling (ทำงานได้โดยไม่มี)
openpyxl

# Machine Learning (ทำงานได้โดยไม่มี แต่จะไม่มี AI features)
# scikit-learn
# sentence-transformers
# torch
# transformers

# หมายเหตุ:
# - แอปจะทำงานได้โดยไม่มี ML packages แต่จะไม่มีฟีเจอร์ AI
# - สำหรับฟีเจอร์ AI ให้ติดตั้งเพิ่ม: pip install sentence-transformers torch
# - ถ้าติดตั้งไม่ได้ ให้ลองใช้ conda แทน pip

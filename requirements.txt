# แชทบอทสูตรอาหารไทยพร้อมระบบวิเคราะห์โภชนาการขั้นสูง
# ไฟล์ Dependencies ที่ปรับปรุงแล้วสำหรับการรองรับ API

# Dependencies หลัก
streamlit
pandas
numpy

# Machine Learning & NLP (ปรับปรุงแล้ว)
sentence-transformers
scikit-learn
torch
transformers

# การแสดงผลข้อมูล (ปรับปรุงแล้ว)
plotly
matplotlib
seaborn
altair  # ไลบรารีสำหรับการแสดงผลเพิ่มเติม

# HTTP requests และ APIs (ปรับปรุงสำหรับการรวม API)
requests
urllib3
httpx  # HTTP client สมัยใหม่ที่รองรับ async
aiohttp  # Async HTTP client

# ฐานข้อมูล (ปรับปรุงแล้ว)
# sqlite3 มีมาในตัวกับ Python แต่เพิ่มเครื่องมือที่เกี่ยวข้อง
sqlalchemy  # ชุดเครื่องมือฐานข้อมูลขั้นสูง
alembic     # เครื่องมือ Database migration

# JSON และการประมวลผลข้อมูล (ปรับปรุงแล้ว)
jsonschema
pydantic   # การตรวจสอบข้อมูลและการจัดการการตั้งค่า
orjson      # ไลบรารี JSON ที่เร็ว

# Logging และ utilities (ปรับปรุงแล้ว)
python-dateutil
pytz
loguru      # Advanced logging
rich       # Rich text และการจัดรูปแบบที่สวยงาม

# Async และ Performance (ใหม่)
asyncio-throttle  # Rate limiting สำหรับ async operations
tenacity          # Retry library
backoff           # Exponential backoff

# Caching (ปรับปรุงแล้ว)
diskcache    # Disk-based caching
cachetools   # Memory caching utilities

# การพัฒนาและการทดสอบ (ปรับปรุงแล้ว)
pytest
pytest-cov
pytest-asyncio  # รองรับการทดสอบ Async
black
flake8
mypy         # Type checking

# การปรับปรุงประสิทธิภาพ (ปรับปรุงแล้ว)
numba       # JIT compiler สำหรับการคำนวณเชิงตัวเลข
psutil       # ระบบและ process utilities

# การประมวลผลภาษาไทย (ปรับปรุงแล้ว)
pythainlp    # การประมวลผลภาษาธรรมชาติไทย
attacut      # การตัดคำไทย
# thai-segmenter  # การตัดคำไทยทางเลือก (ปิดใช้งานเพื่อความเสถียร)

# Utilities เพิ่มเติม (ใหม่)
tqdm        # Progress bars
python-dotenv  # การจัดการ Environment variable
click           # การสร้าง Command line interface
tabulate        # การแสดงผลตารางที่สวยงาม
colorama        # ข้อความสีใน terminal

# Web scraping (สำหรับการรวบรวมข้อมูลสูตรอาหารที่อาจเกิดขึ้น)
beautifulsoup4
lxml

# การประมวลผลภาพ (สำหรับการวิเคราะห์ภาพสูตรอาหารที่อาจเกิดขึ้น)
Pillow       # Python Imaging Library

# การส่งออกข้อมูลและการรายงาน (ปรับปรุงแล้ว)
openpyxl     # การจัดการไฟล์ Excel
xlsxwriter   # การสร้างไฟล์ Excel
jinja2       # Template engine

# การปรับใช้งานและ production (ทางเลือก)
gunicorn    # WSGI HTTP Server

# Security (ปรับปรุงแล้ว)
cryptography       # ไลบรารี Cryptographic
pyjwt              # การใช้งาน JSON Web Token
bcrypt             # การ hash รหัสผ่าน

# การจัดการ Configuration (ปรับปรุงแล้ว)
omegaconf          # การจัดการ Configuration

# Type hints และ validation (ปรับปรุงแล้ว)
typing-extensions  # Extended type hints
pydantic-settings  # การจัดการ Settings ด้วย Pydantic

# หมายเหตุ: บาง packages ที่ทำเครื่องหมายเป็น "ทางเลือก" สามารถติดตั้งตามความต้องการ
# สำหรับการติดตั้งทั้งหมด:
# pip install -r requirements.txt

# สำหรับการติดตั้งแบบขั้นต่ำ (คุณสมบัติหลักเท่านั้น):
# ถ้าต้องการแบบขั้นต่ำ ให้ใช้เฉพาะ packages เหล่านี้:
# streamlit==1.45.0
# pandas==2.2.0
# numpy==1.26.0
# sentence-transformers==2.5.0
# scikit-learn==1.4.0
# plotly==5.18.0
# requests==2.31.0
# python-dateutil==2.9.0
# tqdm==4.66.0
# python-dotenv==1.0.0
